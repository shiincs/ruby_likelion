<!DOCTYPE html>
<html>
  <head>
    <title>TwitterApp</title>
    <%= csrf_meta_tags %>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom box-shadow">
      <h5 class="my-0 mr-md-auto font-weight-normal">Twitter</h5>
      <nav class="my-2 my-md-0 mr-md-3">
        <a class="p-2 text-dark" href="/">홈으로</a>
        <a class="p-2 text-dark" href="/new">글쓰기</a>
      </nav>
    </div>
    <div class="container">
      <% flash.each do |k, v| %>  <!-- flash는 휘발성 cookie -->
        <div class="alert alert-<%= k %>"><%= v %></div>
        <script>
          toastr.<%= k %>("<%= v %>");
        </script>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
